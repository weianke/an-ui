import { withInstall } from '@an-ui/utils';
import Collapse from './Collapse.vue';
import CollapseItem from './CollapseItem.vue';

export const AnCollapse = withInstall(Collapse);
export const AnCollapseItem = withInstall(CollapseItem);

export * from './types';
